# Everything-Wine-Docker

Docker container running Everything search index through Wine.

## Features
- Headless operation
- Persistent storage
- Automatic updates
- Resource management

## Usage
```bash
docker-compose up -d
```